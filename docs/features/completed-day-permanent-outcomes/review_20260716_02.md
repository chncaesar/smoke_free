# Review 2: completed-day-permanent-outcomes

Date: 20260716
Branch: main

## Summary

本次第二轮审查发现 1 个问题：Critical 0 个，Major 1 个，Minor 0 个。没有阻塞性发现；此前两个 Critical 已解决：金额类成就改为使用 `completedMoneySaved`，金额目标自动完成也改为使用 `completedMoneySaved`，今日进行中日志不再触发这些永久写入。全量替换/PUT/upsert 类数据完整性检查未发现新增风险；未涉及迁移 SQL。

## Issues

### Critical
- None

### Major
- [M1] 目标自动完成写入后没有立即保存 Core Data 上下文；`checkCompletion` 将 `goal.isCompleted` 和 `goal.completedAt` 改为已完成状态，但调用链未执行 `save()`，用户在完成目标后若应用被系统杀死或尚未触发其他保存点，永久目标完成结果可能丢失，重启后目标重新显示为进行中 — `SmokeFree/SmokeFree/SmokeFree/ViewModels/GoalsViewModel.swift:40`

### Minor
- None
